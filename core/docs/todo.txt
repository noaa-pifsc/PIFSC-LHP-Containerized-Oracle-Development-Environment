
to do:

	_ implement branch protection for all CODE forks
	
	
	_ test if the passwords can have special characters in them that would normally require enclosing quotes
		- @, $, %, *, etc.
	

	X fix logs function call - it's missing or not working
		

	
	X implement the cascading includes (specify the parent folder name and it will automatically include the corresponding files - depending on which configuration file is being loaded)
		X implemented in the configuration include file
			X specify the parent folder name as a variable
			X run a function to include the upstream parent configuration before the current configuration is loaded (recursive processing with array elements added after the recursive function is processed

	X print out the hierarchy for the user so they are aware of the inheritance relationship
		echo "***************"
		echo "The project hierarchy, in order from parent to dependent fork is: 
		echo "***************"

	X implement CAS parameterized create schemas/apex user scripts

	X update CODE function for DB_MAPPING (allow multiple secrets to be injected)
	
	
	X can we use a pointer approach to all of the loops? YES
		X maintain the .active_project and the $parent_project_folder variables.
			X The framework can automatically start at the .sactive_project folder and read the parent_project_folder variable from the deepest fork, then move to the parent folder and repeat the process
			
			X This can automatically build the PROJECT_INHERITANCE by prepending each array element to the start of the array so the first element is the highest parent, and the last element is the deepest fork
				X first element added is the $parent_folder of the .active_project's parent folder
				X second element added is $parent_folder of the active project
				X third element added is .active_project
			
			X recursive function
				code_shared_define_project_inheritance()
					parameters:
						project_name
						projects folder path
						array name
			
					initial execution:
						parameter is .active_project ACTIVE_PROJECT_NAME variable
						next execution is 
					
					stop condition:
						parent project config file doesn't exist or the variable is blank
						
			


	implement workflow:


		functions:
			X code_shared_run_lifecycle_hook (): loop through PROJECT_INHERITANCE array and run the corresponding client/host/container and pre/post hook script
				_ document the updated scope values



			X implement the hooks for each execution platform (client, host, container)
			
			push DSC CODE changes back to CODE

			
			





		define default configuration
		
		define project configurations

		load default configuration first and then loop through the list of projects and load each project configuration

		then run code to generate compose file array (needs to go through folders but in order for the default project's CODE-ords.yml to be used the script needs the final ORDS_ENABLED variable value)
		

		Run the client/host code depending on the user-specified runtime configuration

		For deployment/shutdown code:



		create separate default/project-specific configuration values that override defaults:
			GIT_URL
			APP_SCHEMA_NAME, etc.

		create separate persistent variable configuration file that is used when the container is built and deployed, the override defaults configuration should be separate since those are converted into env vars and passed to host or to containers:
			PROJECT_INHERITANCE=() - used by the hook processing, or should these all just be cascading where the fork refers to the parent folder?
				if there is no hook defined for it, it would still need to create the file (let's go with the project_inheritance array instead)
			DB_MIGRATION_MAP=() - used by the container scripts
			CUSTOM_ENV_VARS=("APP_SCHEMA_NAME" "ORDS_ENABLED" "TARGET_APEX_VERSION")
			COMPOSE_FILES=("core/templates/base.yml")

			
		
		create two different configuration files for each project
			_ Some are the global runtime configuration variables (e.g. Git URL, ORDS_ENABLED, etc.) that are loaded by the client machine and then transmitted to the host (when applicable) via environment variables. 
				_ this only needs to be loaded during the initial client execution (exclude 
			_ Some are the project inheritance hierarchy that provides the project-specific information - these should be loaded every time for client, host, and container executions
				_ do we need to load this for client, host, and container? -> YES -> this is used to process all pre- and post-deployment hooks
				




		new functions:
			combine the compose_files arrays (build the first elements based on env_name and ords_enabled and then append the rest of the values)
			
			run the pre/post hooks for container deployments/shutdowns
			
			
			




		when the container is built:
			loop through project folders and run any pre run code (prep)
			
			construct the compose file array 
				run the deployment code (using compose file array and global vars)

			then loop through project folders and run any post run code (cleanup)


		when the automated container database deployment script is run:
			when the container script runs it needs to have the necessary environment variables specified when the container is built/run
				When the container scripts run it just needs to loop through the different array arguments and process the steps:
					generate the array of secrets based on the cascading project configurations
					generate the credentials based on the array of secrets and corresponding secret values
					
					execute the container database deployment script that will loop through the array of 
				






	X redo test cases to nest the verification steps under the appropriate headings
		X 5/11/26: use server version

	X test case for test deployment, rem_vol = yes, ords_enabled = no
		X (No Need) This will still remove the apex static volume because it's associated with CODE-db-deploy.yml


	X update the configuration so users can specify a unique container name so it won't conflict with other containers on the same server (could allow multiple people to use a different shared container host for development)
		e.g. GCP shared compute instance running docker, but it will depend on performance

	X for server deployments, should we omit the mount volumes altogether?
		NO

	X separate out the functionality into separate functions with defined naming convention
		X create CODE core scripts that are reused across forks
		
		X implement parameters for all feasible functions that don't change across project-specific implementations
	
		X implement the secrets.sh file
		
		X update the configuration.sh and custom_configuration.sh files

		X remove the .env file altogether
			X drawback is that we can't deploy directly from server
		
		
		
		X create a script that shutsdown the docker container and optionally removes the volumes
		
		
		X export all variables that are in custom_configuration.sh (for server deployments so the client's configuration is the one used for the build)
		
		X determine a more secure way to specify the ORACLE_PWD, exporting this is insecure and it will be available in the inspect for the ORDS and DB containers. 
			X implement docker secrets for ORACLE_PWD and map to all the necessary containers
				X confirm that the secrets are passed in to the container_deploy_database.sh successfully
		
		X combine the deploy and shutdown commands with branching logic for local and server deployments, this will simplify the code management and reduce the amount of calling client and host scripts
		
		X replace the get_array_val() calls with the simple arg_ref element references to simplify the code and make it more readable (global change)
		
		X check if --ignore-pull-failures is necessary for the docker compose pull execution
			--ignore-buildable was used instead to ignore the custom images that need to be built when pulling images

		_ leverage the secret mapping variable and the DOCKER_COMPOSE files to create a parsed secrets local variable that can be used dynamically so we can generate the various connection strings
			_ instead reference the secret value variable names since they provide the path to the secret
		
		_ ** re-examine restart policy for the code-db-ords-deploy container - it should probably retry on failure (5 times?)
		
		
		X created a new cds_shared_export_array_keys() function that exports environment variables from an array of values based on the list of fields
			_ cds_shared_export_env_vars may be obsolete, but this needs to be determined after reviewing CDD and CAD functions
		
		X check the volume deletion functionality on host server
			X shutdown with volume purge
			X deploy with volume purge
	
		
		_ look to automate the deployment of the various database scripts in the CODE forks
			_ we could specify the relative script path and then the db secrets (username, password) that are supplied with the database script.  If they can be specified in array arguments then a bash function might be able to loop through the database scripts and specify the credentials
			

		_ place the .deploy_ready in a subfolder to not clutter up the apex static files folder
		
		_ create separate secret variables for the various apex and ords accounts 
			apex admin, ords_public_user, apex_public_user, 


		folder structure:
			README.md
			CODE_core
				functions
				scripts
				templates
				modules
					CDS
			projects
				orchestration_manifests
					project-name
						project.env (recursively references parent project.env)
							- load parent .env file
							- add project-name (folder) to PROJECT_INHERITANCE
							- add sql path, sql script, username, password to DB_MIGRATION_MAP (for each database script that is executed)
							- add any custom environment variables that will be passed via environment variable to the other scripts to CUSTOM_ENV_VARS
							- add any custom .yml files to COMPOSE_FILES 
						overrides.yml
						hooks
							pre-deploy.sh
							post-deploy.sh
				modules
					project-name (git submodules)



			Changes:
				containerized_oracle_development_environment/CODE_core_scripts -> core/scripts/functions 
				containerized_oracle_development_environment/deployment_scripts -> core/scripts 
				containerized_oracle_development_environment -> core/build
				containerized_oracle_development_environment/secrets -> secrets
				modules/CDS -> core/modules/CDS
				docs -> core/docs

		
			to do:
				X .dockerignore
		
		
		_ extra scenario where someone just wants a sandbox, they can fork it themselves and then make any changes they want
			_ May need to rephrase "forked repository" to differentiate between cascading dependencies and just a sandbox fork someone wants to use without implementing the dependency inheritance design
			
		
		_ ** create a new function to load the secret value into a variable so the /run/secrets/ path isn't hardcoded everywhere
		
		
		we need separate setup instructions for:
			CODE deployments (unforked)
			CODE forks
			
			
		
		
		
		business rules:
			DB_SCRIPTS_MAP specification (which values go where in the delimited string)
                -   **Note: the path of the .sql scripts should be an absolute path, the $BUILD_PATH folder path value (/core/build/) can be used when defining the path

			project_hierarchy_config.sh
				X PROJECT_INHERITANCE - determines the order of the dependencies
					determines the order in which the hooks are executed
				X CUSTOM_ENV_VARS - it will export these environment variables based on the corresponding bash variables so they are available to the containers and client/host scripts
				X SECRET_MAPPING_ARR - it will automatically define these elements as secrets (with a prefix of COMPOSE_PROJECT_NAME to help ensure they are unique) so they are available in the corresponding containers 
					X -   The array element values should correspond to the secret variables specified in secrets.sh. The array element names should correspond to the actual secret names used in the given container.

				X COMPOSE_FILES - it will include any additional compose files when the CODE framework is deployed
					X -   \*Note: The .xml file must be defined relative to the /core/build folder  

				X secrets.sh needs to define bash variable names that match the SECRET_MAPPING_ARR array values to be automatically defined as secrets

			X Execution order:
				X For all of the project-specific functionality (hooks, compose files, database deployment scripts) 		
				they will be executed in the order they are added to the array (highest parent first) since the individual configuration files have their parent project's configuration executed before the forked project's configuration. 				
				X For example if project A is forked from CODE, project B is forked from project A then when the compose files are executed A's configuration variables are defined and the specified scripts are executed before project B's. 

			
			container application setup
				-   To allow multiple developers to use CODE concurrently on the same container host, update the three variables identified in the top section: COMPOSE_PROJECT_NAME, DB_HOST_PORT, and ORDS_HOST_PORT to have unique values				
				-   \*Note: When server deployments are executed the custom_container_config.sh values on the client machine are used instead of the custom_container_config.sh value that are saved in the server's cloned CODE repository, the values are transmitted to the server using environment variables

		
			each time the ords/apex container is started it will set the admin passwords again using the secrets.sh ORACLE_PWD variable
			
		
				        -   The project-specific global configuration variables are defined and the scripts are executed in reverse order from the highest parent project (a direct fork of the [CODE](#code-version-control-information) project) to the current fork

			_ change management/version control business rules (reference by CODE Implementation Procedure section)
			    -   The files in the [/core](../) folder should ever be modified in any forks, these files should only be modified by the [CODE repository](#code-version-control-information) and then those upstream changes can be pulled by the individual forked repositories. 
				-   The only project folder within the [/projects](../../projects) folder that should be modified within that forked repository was the one created when the CODE repository was forked. If there are changes necessary for a different project folder those changes should be made in the corresponding forked repository and then those upstream changes can be pulled into the corresponding forked repository.

			
			hooks:
				-   The "hooks" folder contains any pre- or post-processing scripts that follow the standard naming convention that can automatically run in the specified scope (client, host, container) 
				create a .sh file for any of the scenarios that require custom code:
					For example pre_client_hook.sh will automatically run before the client processing 

			X runtime configuration
				X -   APP_SCHEMA_NAME is the database schema that will be used to check if the database schemas have been installed, this only applies to the [development runtime scenario](#development)
				X -   DB_IMAGE is the path to the database image used to build the database contianer (db container)
					-   This can be updated if a specific version of the database is required
				X -   ORDS_IMAGE is the path to the ORDS image used to build the ORDS/Apex container (ords container)
					-   This can be updated if a specific version of ORDS is required
				X -   TARGET_APEX_VERSION is the version of apex that will be installed/upgraded to when the CODE framework instance is deployed
				X -   COMPOSE_PROJECT_NAME - used to help ensure that the same CODE project can run concurrently on the same host server 
					X -   for server deployments this will determine the container name and the folder name for the working copy of the repository on the server
				X -   ORDS_ENABLED (yes/no) defines if the ORDS service is enabled (required for Apex/ORDS functionality)
				X -   DB_HOST_PORT is the port that the Oracle database will be available from on the container host
				X -   ORDS_HOST_PORT is the port that ORDS will be available from on the container host

				X -   Host deployment configuration:
					X -   GIT_URL is the git project that will be used to build and deploy the CODE containers
					X -   PRIV_USER is the privileged user on the docker host that will be used to build and deploy the CODE containers
					X -   HOSTNAME is the hostname of the docker host where the CODE containers will be deployed
					
	
	document

		_ document that apex dependencies can be implemented as well, they use the same ords server

		_ SOP for defining the project_parent_config for a new forked folder.  You can simply copy the value from the .active_project file and use that before that file is updated to the new fork's project folder

		_ diagram and document the overall execution process for the local and server deployments so we can also identify the hooks, where they are run, and the main action associated with each scope
		
		_ document the new project_parent_config.sh
			_ defines the project folder for the parent, this must be defined in order for the dependencies to be deployed properly
	
		_ document the different scenarios:
			_ fork and customize for data system, forking to build on dependencies to leverage the existing forks
			_ use as-is to provide a blank dev/test database instance that a developer could use for local offline development
			
			
			_ document the runtime arguments and how they affect the implementation (ORDS_ENABLED)
			_ document the user-defined runtime arguments and how they affect the implementation
			
			
			document the different containers implemented in the setup
			
			document the strategy for forking and how to choose a suitable fork
				review the database dependencies and fork them in order starting with the base dependency and define subsequent forks for each 
			
	
			document code management strategy
				for the CODE project it can change anything in the core folder but it shouldn't change any other files/folders
				for a given CODE fork, the only folder that can be modified is the corresponding /projects subfolder
					-   If the files in another projects subfolder need to be updated the changes should be made directly in the corresponding repository and then the upstream changes can be pulled into the current CODE fork. 
				
			.active_project is set to ignore upstream changes because it is always intended to change when a new fork is created
				
				
			should we define a "business rules" section? YES
				link to specific business rules in the documentation where they make sense
				
			
				
	
	
		Implementation:
			New:
				create fork

				create feature branch
				
				X change .active_project to set the ACTIVE_PROJECT_NAME value to folder name in projects/ folder
				copy the code scripts template to the projects folder and rename it to the $ACTIVE_PROJECT_NAME
				add git submodules in the projects/ACTIVE_PROJECT_NAME/modules folder and reference them in the project_hierarchy_config.sh configuration file accordingly (.yml files
				Update projects/ACTIVE_PROJECT_NAME/build/X.yml to define the secrets and any overrides for docker containers that are being added
				update projects/ACTIVE_PROJECT_NAME/config/project_hierarchy_config.sh with the basic information about the forked CODE implementation:
					PROJECT_INHERITANCE should add an element with the ACTIVE_PROJECT_NAME 
					DB_SCRIPTS_MAP: add an array element for each automated database deployment script that will be run on deployments with the corresponding secret names
						**Note: the path of the .sql scripts should be relative to the BUILD_PATH folder path value
					COMPOSE_FILES to add any .yml files from the project's repository and/or any configuration overrides
						**Note: the path of the .yml files should be relative to the BUILD_PATH folder path value
					SECRET_MAPPING_ARR array elements/values that correspond to the secret name (element name) and the corresponding bash variable defined in the secrets.sh as the value
					CUSTOM_ENV_VARS array values for any custom environment names that need to be transmitted to the container on startup
				update projects/ACTIVE_PROJECT_NAME/config/project_runtime_config.sh with any global configuration variable values that are specific to this project (e.g. COMPOSE_PROJECT_NAME, APP_SCHEMA_NAME, GIT_URL, ORDS_ENABLED, TARGET_APEX_VERSION, etc.)
				
				create any special hooks in the projects/ACTIVE_PROJECT_NAME/hooks folder with the following naming convention:
					event_context_hook.sh where event is pre/post and context is client, host, container
				secrets.sh -> define the required secret variable values for the given CODE fork
				
				** X do we define a variable that defines the parent folder so the script can dynamically include the configuration file of the parent folder instead of hard coding the reference? -> YES
					
				X ** do we also establish a repo_root variable that will make the relative paths easier to define (less ../ will also help readability)

				test dev and test deployments
				
				merge feature branch
				
				pull upstream changes as new releases are deployed
					Notify via watching a project and/or checking PRI for new releases
				
			pull upstream changes
			
	
	
	
	
	
		**** _ clearly indicate this is only intended for development and testing purposes
			* Not security hardened, encouraged not to work with sensitive data in a shared environment (anyone with docker privileges can connect to your containers and access the data within it)
			
		_ contribution guidelines for this project
			how to fork and submit pull requests or attempt to push changes upstream (think through the scenarios)
			which files to not change in a given project and which ones are ok
			
		
		X the custom_container_config.sh configuration that is stored on the client machine is used when deploying to the server (passed via environment variables)
		
		X docker swarm init to initialize docker swarm
	
		X secrets.sh will only set the oracle admin password on the initial DB container run, after that it is only used to connect to the database
			X same goes for the data/app schema deployment scripts
	
		X new installations can take up to 30 minutes depending on the resources allocated to the docker host
		
		
		_ when rem_vol = yes it will remove all volumes associated with the stack
	
		_ document the difference between dev and test scenarios
		
		_ if there is a scenario where the volumes need to be cleared manually - document it and provide the script and parameters to use
			_ for example - apex static files are a higher version than the target apex version and the installed database version (think about these more)
			
		_ ** document that the only scenario where the volumes need to be removed is when the database needs to be reset
			Either because the data needs to be replaced/re-seeded or a lower apex version than what is installed on the databse needs to be used:
			
				explicit shutdown/removal:
					bash ./client_execute_CODE_scripts.sh shutdown dev local yes
				explicit shutdown/removal/deploy:
					bash ./client_execute_CODE_scripts.sh deploy dev local yes
			
		_ the apex admin account and ORDS_PUBLIC_USER accounts are reset on each deployment using the secrets.sh file
		
		_ document the stateless APEX installation, can switch between dev and test deployments and the oracle apex admin password, ords service account, and apex service account are all set using the defined $oracle_admin_pwd secret value (secrets.sh)
		
		_ document why the apex configuration file is generated instead of using the CLI ords config commands
			it took several seconds for each command, and this added over a minute to the startup time
		
		target apex version (TARGET_APEX_VERSION)
		current apex version (DB)
		current apex version (static files)
		
		_ merge protection: .active_project defines the name of the active project that corresponds to the folder name in the /projects folder
			
			
		_ document that the secrets.sh is cumulative and so the single file needs to contain the secret definitions for all schemas involved 
			_ should we have the secrets template change with the project? yes, would this be a case where we would want to auto reject changes?
				There is a chance that the schemas will change in an upstream update - for example if an application schema is implemented it could add another schema during the deployment that needs to be continued down the chain.  These should be very rare, maybe we just have this be a rare merge case?
			
			requires one-time setup (https://gemini.google.com/app/8a7f53fd936961a5): git config merge.ours.driver true
		
		business rules:
			when the apex installation has been completed 
		
	
		_ review the sections defined for CDD/CAD and implement the ones that are appropriate
		X naming conventions
		X implementation instructions


	X implement CDD/CDS functions where appropriate	
		
		
	X change the docker compose deploy and shutdown functions to use the list of .yml files with a "-f" flag before it
		
		
		
		
	X try to reuse CAD for server deployments

		
	X try to get CODE running on a linux host
		X resolving issue with mounted volume on a linux host (switch to named volume)
	
	
	X update boilerplate functions to separate them into the core_engine directory and make all variables arguments (no references to global configuration variables)
		* these functions are intended to be upgraded over time as requirements change or improvements are made
		
	
	X define boilerplate functions for CODE projects that are not intended to change across CODE forks
		X have these functions call the custom functions that will change across CODE forks
	
	
	
	how do we handle the apex static volume?
		test defines the apex static volume and ords volume
			if the dev volumes were not removed then the apex static files will be there including the temp file that causes ords to start right away
			solution: automatically clear the associated volumes when test version starts up
		
		dev defines all 3 volumes
			when it restarts the apex static volume will still be there, so ords will start right away because of the temp file
			
			if a test run executes and then the dev executes again then the db volume will be there but the other 2 volumes won't be. 
				Apex will be installed on the db and the script will detect the absence of the apex static files and copy those files again

				_ * add a test case for this
				

		

		dev
			deploy 
				rem_vol = yes (same as redeploy with rem_vol = yes)
					remove all volumes
					create all volumes
					create all services
			deploy
				rem_vol = no
					retain all volumes
					create all services
					
		test
			deploy
				rem_vol = yes in all cases
					remove apex, ords volumes
					create all services
					
		if you have an existing db volume and no apex/ords volumes
			it will check to see if the target apex version is installed on the database (it will validate as usual -> downgrade, upgrade, same version)
				same version: install static files in volume
				upgrade: install apex on db and static files in volume
				downgrade: error
			if apex is not installed on the db:
				install: install apex on db and static files in volume
				
		** if you have an existing db volume and the existing apex/ords volumes from the test deployment, it may break the containers if there is a mismatch between the apex version in the volumes and the db
			_ should we update the core CODE to detect this scenario?
				_ **compare the apex version in the static files to the apex version in the database, if they don't match then initiate a reinstall of the apex static files to match the version in the database
	
	
	
	
	X how to guarantee unique container naming so multiple CODE projects can run on the same server at the same time
		X need to also change the port number to avoid conflicts
			X make the updates custom_container_config.sh
				COMPOSE_PROJECT_NAME
				DB_HOST_PORT
				ORDS_HOST_PORT


		_ test concurrent CODE instances on docker host
		
	_ implement secrets
		X cds_shared_build_deploy_container_stack was migrated and some arguments were changed (build_path is new, should be blank for single image builds)
		
		
		_ implement dynamic parsing of secrets into the parsed_secrets array so they can be used to generate the connection strings
			_ update DSC create use SQL to parameterize the password in the create schema scripts
			_ inject the password into the script
		
		
		
		X avoid collisions with secrets (prefix with $COMPOSE_PROJECT)
		
	
		
	X use array arguments for CODE functions
		X passthrough the argument arrays to functions from within functions when appropriate
		
		X check if the docker compose calls should be updated in CDS to allow multiple -f arguments
			X using COMPOSE_FILE special environment variable
	X Update CODE to use CDS directly again
		_ Update CDS on all dependent projects:
			_ CDD:
				_ general improvements:
					_ use ${FUNCNAME[0]} instead of hardcoding the function names in error messages
					_ update cds_host_deploy_container function calls to set persistent_container flag
					_ cds_host_cleanup_container_source_folder() accepts two parameters now (source_dir, target_user)
					_ update the cds_shared_get_array_val() function calls to use the arg_ref approach
					_ cds_shared_remove_container_stack() was updated to accept a third argument: remove_volumes
						_ could use this for the LCE to remove the verification volumes

				_ LHP
				_ PRI
				_ LCE
			_ CAD:
				_ general improvements:
					X use ${FUNCNAME[0]} instead of hardcoding the function names in error messages
					X update cds_host_deploy_container function calls to set persistent_container flag
					X cds_host_cleanup_container_source_folder() accepts two parameters now (source_dir, target_user)
					X ** check cds_shared_deploy_container_stack -> added rem_vol argument
					X cds_shared_shutdown_container_compose -> rem_vol argument
					_ update the cds_shared_get_array_val() function calls to use the arg_ref approach
					_ cds_shared_remove_container_stack() was updated to accept the following arguments:
						rem_vol
						build_path
						compose_path
						
						_ change to use array arguments (since there are 5 args now)
						
						_ this should be implemented as an option to allow volumes to be easily removed
					_ update 
				_ PRI
				_ SIA
				_ PLW
		
		
		
	X (4/14/26) troubleshoot why skip_file_install is 0
		
	X replace the way the apex_version_compare	
		
		
		
	4/20/26:
		Working on implementing the host deployment for CODE, need to pass/parse STDIN. 
			Try to use CDD as the starting point
				It encodes/decodes STDIN, it does not use secrets, it does not use docker stack so it is probably the most well-suited for this
					host_deploy_CODE.sh: proj_host_deploy_container()
						model after host_deploy_database.sh
					host_deploy_CODE_elev_privs.sh: proj_host_deploy_container_elev_privs()
						model after host_deploy_database_elev_privs.sh

					cdd_host_deploy_database_execute_container_script() was copied and modified in proj_


		
	documentation:
		need to sync up the SECRET_MAPPING_ARR and .env file secrets
		create server_deploy_config.sh in deployment_scripts/config using the server_deploy_config.template.sh

git bash commands:


	# change into repo directory:
	cd /c/Users/jesse.abdul/Documents/Version\ Control/Git/PIFSC-Containerized-Oracle-Development-Environment/core/scripts/client_scripts



	# Dev:

		# build and deploy the development scenario of the CODE (local)
		bash ./client_execute_CODE_scripts.sh deploy dev local

		# build and deploy the development scenario of the CODE (server)
		bash ./client_execute_CODE_scripts.sh deploy dev server

		# establish the connection to the docker server mapping the default ports so those access can be access on the client machine (make sure there are no containers using localhost that will cause a conflict)
		ssh -N -L 1521:localhost:1521 -L 8181:localhost:8181 dev_docker


		# shutdown the container (local) and retain volume
		bash ./client_execute_CODE_scripts.sh shutdown dev local no


		# shutdown the container (local) and remove volumes
		bash ./client_execute_CODE_scripts.sh shutdown dev local yes

		# shutdown the container (server) and retain volume
		bash ./client_execute_CODE_scripts.sh shutdown dev server no


		# shutdown the container (server) and remove volumes
		bash ./client_execute_CODE_scripts.sh shutdown dev server yes


	# Test:

		# build and deploy the test scenario of the CODE (local)
		bash ./client_execute_CODE_scripts.sh deploy test local

		# build and deploy the test scenario of the CODE (server)
		bash ./client_execute_CODE_scripts.sh deploy test server

		# establish the connection to the docker server mapping the default ports so those access can be access on the client machine (make sure there are no containers using localhost that will cause a conflict)
		ssh -N -L 1521:localhost:1521 -L 8181:localhost:8181 dev_docker


		# shutdown the container (local) 
		bash ./client_execute_CODE_scripts.sh shutdown test local no

		# shutdown the container (server)
		bash ./client_execute_CODE_scripts.sh shutdown test server no

troubleshooting tips:











Key:
	L => laptop
	V => Virtual Machine

testing:
	X server:
		X deploy
			X test
				X ORDS_ENABLED:
					X yes
						X installs db, ords, apex from scratch
							X verify ORDS/Apex login
							X verify DB connectivity
								X Create temp table and insert data
						X validation:
							X invalid TARGET_APEX_VERSION (format fails - 24.3.1)
							X incorrect TARGET_APEX_VERSION (does not exist - 24.6)
						X REM_VOL
							X yes - remove apex static volume
							X no - retain all volumes
					X no
						X REM_VOL
							X yes - remove apex static volume
								X verify DB connectivity
							X no
								X verify DB connectivity
			X dev
				X ORDS_ENABLED:
					X yes
						X installs db, ords, apex from scratch
							X verify ORDS/Apex login
							X verify DB connectivity
								X Create temp table and insert data
						X (same TARGET_APEX_VERSION) resume db, ords, apex
							X verify ORDS/Apex login
							X verify DB connectivity
								X verify temp table
						X (increase TARGET_APEX_VERSION) resume db, ords, apex
							X verify ORDS/Apex login
							X verify DB connectivity
								X verify temp table
						X REM_VOL
							X yes - remove all volumes
								X verify ORDS/Apex login
								X verify DB connectivity
									X Create temp table and insert data
							X no - retain all volumes
						X validation:
							X decrease TARGET_APEX_VERSION (downgrade error)
							X invalid TARGET_APEX_VERSION (format fails - 24.3.1)
							X incorrect TARGET_APEX_VERSION (does not exist - 24.6)
					X no - ORDS container does not run
						X initial
							X verify DB connectivity
							X Create temp table and insert data
						X restart
							X verify DB connectivity
							X verify temp table
						X REM_VOL
							X yes - remove all volumes
								X verify ORDS/Apex login
								X verify DB connectivity
									X Create temp table and insert data
							X no - retain all volumes
		X shutdown
			X test
				X REM_VOL
					X yes
						ORDS_ENABLED:
							X yes: remove apex static volume
							X no: remove apex static volume
					X no
						ORDS_ENABLED:
							X yes: retain volumes
							X no: retain volumes
			X dev
				X REM_VOL
					X yes
						ORDS_ENABLED:
							X yes: remove apex static and db volumes
							X no: remove apex static and db volumes
					X no
						ORDS_ENABLED:
							X yes: retain volumes
							X no: retain volumes

		X special scenarios:
			X ORDS enabled:
				X 1 (upgrade apex static files version only):
					X deploy dev - version 23.1
						X verify ORDS/Apex login
						X verify DB connectivity
							X create table/insert test data
					
					X test dev rem_vol = no
						(install version 23.2: files and db)
						X verify ORDS/Apex login
						X verify DB connectivity
					
					X deploy dev - version 23.1
						(upgrade files only, no need to install database)
						X verify ORDS/Apex login
						X verify DB connectivity
							X verify database values from initial dev run are still in the database
					
				
				X 2 (upgrade db apex version only): 
					X deploy dev - version 23.1
						X verify ORDS/Apex login
						X verify DB connectivity
							X create table/insert test data
					
					X remove apex static volume: 
						(shutdown test rem_vol = yes)
						X confirm apex static dir volume is removed

					X deploy dev - version 23.1
						(install static files only)
						X verify ORDS/Apex login
						X verify DB connectivity
							X verify database values from initial dev run are still in the database
					

			X ORDS not enabled:
				X 1 (verify dev DB data is retained):
					X deploy dev
						X verify DB connectivity
							X create table/insert test data
					
					X deploy test
						X verify DB connectivity
					
					X deploy dev
						X verify DB connectivity
							X verify database values from initial dev run are still in the database
					
	_ local:
		L deploy
			L test
				L installs db, ords, apex from scratch
					L verify ORDS/Apex login
					L verify DB connectivity
				L ORDS_ENABLED:
					L yes
						L validation:
							L invalid TARGET_APEX_VERSION (format fails - 24.3.1)
							L incorrect TARGET_APEX_VERSION (does not exist - 24.6)
					L no
						L verify DB connectivity

				L REM_VOL
					L yes - remove all volumes
						L ORDS enabled = yes
							L verify ORDS/Apex login
							L verify DB connectivity
								L Create temp table and insert data
						L ORDS enabled = no
							L verify DB connectivity
					L no - retain all volumes
			L dev
				L installs db, ords, apex from scratch
					L verify ORDS/Apex login
					L verify DB connectivity
						L Create temp table and insert data
				L (same TARGET_APEX_VERSION) resume db, ords, apex
					L verify ORDS/Apex login
					L verify DB connectivity
						L verify temp table
				L (increase TARGET_APEX_VERSION) resume db, ords, apex
					L verify ORDS/Apex login
					L verify DB connectivity
						L verify temp table
				L ORDS_ENABLED:
					L yes
						L validation:
							L decrease TARGET_APEX_VERSION
							L invalid TARGET_APEX_VERSION (format fails - 24.3.1)
							L incorrect TARGET_APEX_VERSION (does not exist - 24.6)
					L no - ORDS container does not run
						L initial
							L verify DB connectivity
							L Create temp table and insert data
						L restart
							L verify DB connectivity
							L verify temp table
						
				L REM_VOL
					L yes - remove all volumes
						L verify ORDS/Apex login
						L verify DB connectivity
							L Create temp table and insert data
					L no - retain all volumes
					
		L shutdown
			L test
				L REM_VOL
					L yes (deletes only apex static volume)
						L ords enabled = yes
							
						L ords enabled = no
							
					L no (retain all volumes)
						L ords enabled = yes
							
						L ords enabled = no
			
			L dev
				L REM_VOL
					L yes
						ORDS_ENABLED:
							L yes: remove apex static and db volumes
							L no: remove apex static and db volumes
					L no
						ORDS_ENABLED:
							L yes: retain volumes
							L no: retain volumes
		
		L special_ scenarios:
			ORDS enabled:
				L 1 (upgrade apex static files version only):
					L deploy dev - version 23.1
						L verify ORDS/Apex login
						L verify DB connectivity
							L create table/insert test data
					
					L test dev rem_vo_ = no
						(install version 23.2: files and db)
						L verify ORDS/Apex login
						L verify DB connectivity
					
					L deploy dev - version 23.1
						(upgrade database only, no need to install static files)
						L verify ORDS/Apex login
						L verify DB connectivity
							L verify database values from initial dev run are still in the database
					
				
				L 2 (upgrade db apex version only): 
					L deploy dev - version 23.1
						L verify ORDS/Apex login
						L verify DB connectivity
							L create table/insert test data
					
					L remove apex static volume: 
						(shutdown test rem_vol = yes)
						L confirm apex static dir volume is removed

					L deploy dev - version 23.1
						(instal_ static files only)
						L verify ORDS/Apex login
						L verify DB connectivity
							L verify database values from initial dev run are still in the database
					

			L ORDS not enabled:
				L 1 (verify dev DB data is retained):
					L deploy dev
						L verify DB connectivity
							L create table/insert test data
					
					L deploy test
						L verify DB connectivity
					
					L deploy dev
						L verify DB connectivity
							L verify database values from initial dev run are still in the database
