X merge upstream changes from DSC 


X test the different deployment/restart scenarios (tests completed on 12/17/25)
	X dev
		X new deployment
			X db
				X CEN_UTILS schema works
				X DSC DB schema works
				X login to SYS
				X login to SYSTEM

		X restart
			X Does not redeploy DB 
			X db
				X CEN_UTILS schema works
				X DSC DB schema works
				X login to SYS
				X login to SYSTEM

	X test (does not save database volume)
		X db
			X CEN_UTILS schema works
			X DSC DB schema works
			X login to SYS
			X login to SYSTEM


git bash commands:

	# Dev:

		# stop and remove CODE containers and delete volumes
		cd /c/Users/jesse.abdul/Documents/Version\ Control/Git/PIFSC-LHP-Containerized-Oracle-Development-Environment
		
		docker compose \
		  --env-file ./docker/.env \
		  -f docker/CODE-db-deploy.yml \
		  -f docker/CODE-db-named-volume.yml \
		  -f docker/CODE-ords.yml \
		  -f docker/custom-docker-compose.yml \
		  down -v

		
		# build and deploy the development scenario of the CODE
		bash ./deployment_scripts/build_deploy_project.sh dev
	
	# Test:

		# stop and remove CODE containers and delete volumes
		cd /c/Users/jesse.abdul/Documents/Version\ Control/Git/PIFSC-LHP-Containerized-Oracle-Development-Environment
		
		docker compose \
		  --env-file ./docker/.env \
		  -f docker/CODE-db-deploy.yml \
		  -f docker/CODE-ords.yml \
		  -f docker/custom-docker-compose.yml \
		  down -v

		# build and deploy the test scenario of the CODE
		bash ./deployment_scripts/build_deploy_project.sh test

